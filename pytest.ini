[pytest]
testpaths = tests
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers -m "not integration"
markers =
    integration: Tests that connect to real AWS resources and require explicit AWS integration configuration
